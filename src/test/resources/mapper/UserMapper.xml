<mapper namespace="test">
    <select id="selectById" parameterType="java.lang.Integer" resultType="com.cn.mybatis.entity.User">
        <!--SELECT * FROM user WHERE id = #{id}-->
        SELECT * FROM user
    </select>
</mapper>